apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: smart-task-manager
    tier: backend
spec:
  selector:
    app: smart-task-manager
    tier: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP